# AWS Site-to-Site VPN 

AWS Site-to-Site VPN creates a secure, encrypted connection between your on-premises network (or another cloud) and your AWS Virtual Private Cloud (VPC). 
It uses IP Security (IPSec) tunnels to establish a private communication channel over the public internet


Key Concepts:
 1. Customer Gateway (CGW)
Represents the on-premises VPN device
Requires: BGP ASN and public IP address (or private IP for Direct Connect)

 2. Virtual Private Gateway (VGW)
Represents the AWS side of the VPN connection Has its own BGP ASN (e.g., 64512)
Can be attached to a VPC or Transit Gateway

 3. VPN Connection
Creates the IPSec tunnels between CGW and VGW
Two tunnels for redundancy
Supports static or dynamic (BGP) routing.

 4. Route Tables & Propagation
VPC route tables must direct traffic to the VPN gateway
With BGP, routes are automatically exchanged

Resources Created:
AWS Virtual Private Gateway
AWS Customer Gateway
AWS Site-to-Site VPN connection (2 tunnels)
Route table entries directing VPC traffic to the VPN

## Project Overview 

This lab simulates a hybrid connection between an on-premises network and AWS using an IPsec Site-to-Site VPN. Since a physical on-prem router/firewall isn't available, a second VPC (or a software router like
strongSwan/OpenSwan on an EC2 instance) stands in as the "on-prem" side.

If this follows on from the Transit Gateway module, the VPN can attach either directly to a VPC (Virtual Private Gateway) or to the Transit Gateway as another spoke — extending the hub-and-spoke topology to
include on-prem.

Two Ways to Terminate the VPN:

VPN → Virtual Private Gateway (VGW) → single VPC Simple, classic setup. Good for a single VPC use case.
VPN → Transit Gateway → multiple spoke VPCs Preferred when you already have a hub-and-spoke setup (like Module 04) — the VPN becomes just another TGW attachment, so on-prem gets the same
routed access as any spoke, controlled through TGW route tables.

Build Steps (typical order) -Reffer the attached LAB Document 

1. Stand up the "on-prem" side
   Either a real on-prem simulator (EC2 + strongSwan) with its own public IP, or use AWS's own second VPC/CGW for practice.

2. Create a Customer Gateway (CGW) in AWS
   Provide the on-prem public IP and BGP ASN (if dynamic).

3. Create the AWS-side termination point
   A Virtual Private Gateway and attach it to the target VPC, or Reference the existing Transit Gateway.

4. Create the Site-to-Site VPN Connection
   Choose static routing (manually list on-prem CIDRs) or dynamic (BGP).
   AWS returns a configuration file with two tunnel endpoints, PSKs, and settings — download it (there's a template for many vendors,
including generic/strongSwan).

5. Configure the on-prem/simulator side
   Apply the downloaded config to strongSwan/Libreswan (IKE version, PSK, encryption/DH settings, tunnel IPs).
   Bring the IPsec tunnel up and confirm Tunnel Status = UP in the AWS console.

6. Enable route propagation / add static routes
  VGW path: enable route propagation on the VPC route table so AWS learns on-prem CIDRs automatically (if BGP), or add static routes.
  TGW path: propagate the VPN attachment's routes into the relevant TGW route table, same as any other spoke.

7. Update security groups / NACLs to allow the expected traffic (e.g., ICMP for testing, plus real app ports) between on-prem and AWS
   CIDR ranges.

8. Test connectivity — ping/traceroute from an instance in the on-prem simulator to an instance in the AWS VPC, and vice versa.

Static vs. BGP (Dynamic) Routing
Static routing: you manually declare on-prem CIDR blocks in the VPN connection config. Simple, but any change on-prem requires a manual update.
BGP routing: on-prem router peers via BGP over the tunnel and advertises/receives routes automatically. More resilient, standard for production, but requires a device that supports BGP (strongSwan can with
a BGP daemon like bird or FRRouting).

Lessons Learned
 # Tunnels stay down until traffic is initiated in some setups — AWS VPN tunnels can show "DOWN" until traffic actually starts flowing; don't assume it's broken just because it's idle.
 # Both tunnels matter — AWS gives you two tunnel endpoints for HA; configure both on the on-prem side, not just one, or you lose redundancy.
 # PSK and IKE parameters must match exactly on both ends — mismatched encryption/DH group/IKE version is the most common cause of a tunnel that never comes up.
 # CIDR overlap between "on-prem" and AWS VPC breaks routing — as with TGW, ranges must be unique.
 # NAT/firewall in front of the on-prem simulator can block IPsec (UDP 500/4500, ESP protocol 50) — make sure those are open.
 # Route propagation must be enabled explicitly on the VPC route table (or TGW route table) — creating the VPN connection alone doesn't add routes automatically unless propagation is turned on.
 # If layering onto Transit Gateway from Module 04, remember the VPN attachment needs to be associated/propagated into the correct TGW route table just like a VPC spoke — see the Association vs. Propagation
notes from that module.

Testing Checklist
 $$  Both VPN tunnels show UP in the AWS console
 $$  VPC/TGW route table shows the propagated on-prem CIDR
 $$  Instance in AWS can ping instance in on-prem simulator
 $$  Instance in on-prem simulator can ping instance in AWS
 $$  Traffic actually traverses the tunnel (check VPNTunnel CloudWatch metrics — TunnelState, TunnelDataIn/TunnelDataOut)
 $$ Failover test: bring down Tunnel 1, confirm Tunnel 2 keeps traffic flowing

---

## AWS Services Used

- Amazon VPC
- Virtual Private Gateway (VGW)
- Customer Gateway (CGW)
- Site-to-Site VPN
- Amazon EC2
- Route Tables
- Security Groups
- Internet Gateway
- CloudWatch

---

## Features

- Secure IPSec VPN connectivity
- Redundant VPN tunnels for high availability
- Support for Static Routing and BGP
- Secure communication between AWS and on-premises resources
- Route propagation through VGW
- Enterprise hybrid cloud networking

---

## Project Objectives

- Configure a Site-to-Site VPN.
- Connect an on-premises network to AWS.
- Understand VPN tunnel establishment.
- Configure routing using Static Routes or BGP.
- Verify encrypted communication.


---

## Validation

- Verify both VPN tunnels are **UP**.
- Confirm routes are propagated correctly.
- Ping EC2 instances from the on-premises network.
- Test application connectivity.
- Monitor tunnel health using CloudWatch.

---

## Learning Outcomes

By completing this project, you will learn:

- AWS Site-to-Site VPN architecture
- Customer Gateway (CGW)
- Virtual Private Gateway (VGW)
- IPSec VPN fundamentals
- Static Routing vs BGP
- Route propagation
- Hybrid cloud networking best practices

---

## Future Enhancements

- Migrate to AWS Transit Gateway VPN.
- Integrate AWS Direct Connect.
- Configure BGP for dynamic routing.
- Enable VPN monitoring and alerts.
- Build a highly available hybrid network architecture.

---


- SD-WAN
- Hybrid Cloud Networking
